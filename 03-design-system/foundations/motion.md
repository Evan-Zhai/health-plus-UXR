# 动效

默认克制。健康福利产品里，夸张动效会像营销，不像可信的账户。

| 场景 | 建议 |
| --- | --- |
| 页面切换 | 系统默认即可 |
| 状态变化 | 不要用会消失的纯色彩闪烁；要留文字 |
| 加载 | 骨架屏，不要无限无说明转圈 |
| 庆祝动画 | 理赔进度不做 |

时长先按 150–250ms，曲线用标准 easing，具体值与工程对齐后写入 token。
